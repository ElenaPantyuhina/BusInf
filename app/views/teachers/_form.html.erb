<%= form_for(@teacher) do |f| %>
  <% if @teacher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@teacher.errors.count, "error") %> prohibited this teacher from being saved:</h2>

      <ul>
      <% @teacher.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label "Фамилия" %><br/>
      <%= f.text_field :teach_surname %>
    </div>
    <div class="field">
      <%= f.label "Имя" %><br/>
      <%= f.text_field :teach_name %>
    </div>
    <div class="field">
      <%= f.label "Отчество" %><br/>
      <%= f.text_field :teach_secondname %>
    </div>
    <div class="select">
      <%= f.label "Кафедра" %><br/>
      <%= f.select :teach_cathedra, Cathedra.all.collect { |c| [c.cath_name, c.id]}, { :include_blank => "Выберите кафедру"} %>
    </div>

  <div class="actions">
    <%= f.submit "Сохранить" %>
  </div>
<% end %>
